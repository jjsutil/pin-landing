# Rule 4 — Language

`docs_language` in `.claude/repo-conventions.md` is law. New docs are written in the
declared language. Touching a doc written in another language means migrating that doc
in the same branch (opportunistic migration) — never a repo-wide migration in an
unrelated PR.
